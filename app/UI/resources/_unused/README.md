# Unused SVG review area

Các SVG trong thư mục này không có mapping trong `AppAssets.qml` và không được runtime tham chiếu. Chúng được giữ lại tạm thời để duyệt trước khi xóa; không dùng trực tiếp asset từ `_unused/`.

