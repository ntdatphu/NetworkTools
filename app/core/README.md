# Core

Dịch vụ dùng chung và các facade QML ổn định. Không thêm CRUD, SQL, template lệnh hoặc protocol worker mới vào đây; chúng thuộc `features` hoặc `infrastructure`. Các adapter hiện có được giữ tạm để không đổi context property/slot QML trong lúc di chuyển.
