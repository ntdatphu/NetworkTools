# External tools

Quản lý catalog và database metadata cho công cụ ngoài. **partial**: QObject/service còn trong `core/runtime.py`; script khởi tạo đã đặt gần feature. QML entry ở settings/content. Không lưu command chứa secret; path executable phải được xác thực trước khi chạy. Test: `test_external_tools.py`. Backlog: tách service/repository khỏi runtime facade.
