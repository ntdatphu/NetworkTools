# Generated SQL aggregates

Hai file SQL trong thư mục này được tạo từ các module schema bằng `python scripts/build_databases.py`. Không sửa trực tiếp; chúng phục vụ kiểm tra schema và các consumer cần một script SQL hoàn chỉnh.
