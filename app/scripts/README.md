# Scripts

- `build_databases.py`: build atomically DB runtime từ schema chuẩn; chạy lại sẽ thay DB đích nên cần backup dữ liệu cần giữ.
- `validate_structure.py`: kiểm tra README, function map trong `bang_ke_hach_cua_viet/`, qmldir, runtime artifact, path tuyệt đối và trạng thái tài liệu; chỉ đọc repo.
