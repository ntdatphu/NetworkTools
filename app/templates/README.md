# Templates

Chứa template nhập/xuất do người dùng tải về, hiện có `EXdevices.xlsx`. Template lệnh cấu hình thuộc `features/<feature>/templates`. Khi đổi cột hoặc cú pháp phải cập nhật version/validation và test tương ứng.
