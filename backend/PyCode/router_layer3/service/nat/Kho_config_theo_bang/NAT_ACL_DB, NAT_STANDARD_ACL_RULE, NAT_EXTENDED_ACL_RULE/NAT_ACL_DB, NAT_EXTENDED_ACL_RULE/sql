-- ==============================================================
-- MODULE: CẤU HÌNH ACCESS CONTROL LIST MỞ RỘNG (EXTENDED ACL)
-- MỤC TIÊU: KIỂM SOÁT LUỒNG TRAFFIC PHỨC TẠP (IP, PROTOCOL, PORT)
-- ==============================================================

-- 1. BẢNG NAT_ACL_DB: Định nghĩa danh sách (Vỏ ACL)
-- Khai báo tên ACL và loại Extended để Tool chọn đúng bảng Rule
INSERT INTO NAT_ACL_DB (
    nat_acl_id, acl_name, acl_type, host, description, success, action_Cfg
) VALUES (
    2,                          -- nat_acl_id: Khóa chính (Liên kết với Rule)
    'ACL_PAT_NV',               -- acl_name: Tên hiển thị trên Router
    'extended',                 -- acl_type: Loại ACL mở rộng
    '192.168.121.101',          -- host: IP Router ARouter1
    'Cho dai 172 ra PAT, nhung chan VPN', -- Ghi chú nghiệp vụ
    0,                          -- success: Trạng thái chờ đẩy
    1                           -- action_Cfg: Đang kích hoạt
);

-- 2. BẢNG nat_extended_acl_rules: Chi tiết các luật lọc (Ruột ACL)
-- Chứa các thông số kỹ thuật chi tiết nhất để sinh lệnh CLI
INSERT INTO nat_extended_acl_rules (
    nat_acl_id, sequence, action, protocol, 
    source, wildcard, src_operator, src_port,
    destination, dest_wildcard, dest_operator, dest_port, success
) VALUES 
-- Luật 10: Chặn traffic từ dải 172 đi đến port 443 của mạng nội bộ 10.x (Chặn VPN)
(2, 10, 'deny', 'tcp', '172.16.0.0', '0.0.255.255', 'eq', 8080, '10.0.0.0', '0.255.255.255', 'eq', 443, 0),

-- Luật 20: Cho phép máy .50 truy vấn DNS (UDP 53) với port nguồn cao (>1024)
(2, 20, 'permit', 'udp', '192.168.1.50', '0.0.0.0', 'gt', 1024, 'any', NULL, 'eq', 53, 0),

-- Luật 30: Cho phép toàn bộ dải 172 đi ra các mạng khác (Trừ các luật đã bị chặn ở trên)
(2, 30, 'permit', 'ip', '172.16.0.0', '0.0.255.255', NULL, NULL, 'any', NULL, NULL, NULL, 0);