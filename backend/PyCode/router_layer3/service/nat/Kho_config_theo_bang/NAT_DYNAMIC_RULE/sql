-- ==============================================================
-- KỊCH BẢN NCKH: PHÂN ĐOẠN MẠNG BẰNG DYNAMIC NAT ĐA POOL
-- (1) Phòng Giám đốc -> Dùng Pool VIP
-- (2) Phòng Nhân viên -> Dùng Pool Normal
-- ==============================================================

-- 1. BẢNG MẸ (NAT_DB)
-- Khởi tạo phiên làm việc cho cấu hình Dynamic NAT
INSERT INTO NAT_DB (nat_id, nat_name, nat_type, host, description, success, action_Cfg)
VALUES 
(2, 'NAT_DYN_MULTI_POOL', 'dynamic', '192.168.121.101', 'Kich ban chia 2 Pool rieng biet', 0, 1);

-- 2. ĐỊNH NGHĨA DANH SÁCH (NAT_ACL_DB & Rules)
-- Tạo vỏ ACL
INSERT INTO NAT_ACL_DB (nat_acl_id, acl_name, acl_type, host, description, success, action_Cfg)
VALUES 
(1, 'ACL_PHONG_GD', 'standard', '192.168.121.101', 'Quyen truy cap Phong Giam Doc', 0, 1),
(2, 'ACL_PHONG_NV', 'standard', '192.168.121.101', 'Quyen truy cap Phong Nhan Vien', 0, 1);

-- Tạo ruột ACL (Quy định dải IP nào thuộc phòng nào)
-- Giả sử dải 192.168.1.0/24 là của Giám đốc, dải 192.168.2.0/24 là của Nhân viên
INSERT INTO nat_standard_acl_rules (nat_acl_id, sequence, action, source, wildcard, success)
VALUES 
(1, 10, 'permit', '192.168.1.0', '0.0.0.255', 0),
(2, 10, 'permit', '192.168.2.0', '0.0.0.255', 0);



-- BẢNG nat_extended_acl_rules: Cấu hình luật lọc theo Protocol và Port
INSERT INTO nat_extended_acl_rules (
    nat_acl_id, sequence, action, protocol, 
    source, wildcard, src_operator, src_port,
    destination, dest_wildcard, dest_operator, dest_port, success
) VALUES 
-- 1. Chặn traffic TCP từ dải 172 (Port 8080) đến hệ thống VPN nội bộ (Port 443)
(2, 10, 'deny', 'tcp', '172.16.0.0', '0.0.255.255', 'eq', 8080, '10.0.0.0', '0.255.255.255', 'eq', 443, 0),

-- 2. Cho phép máy đặc biệt (.50) truy vấn DNS (Port 53) từ các Port nguồn cao (>1024)
(2, 20, 'permit', 'udp', '192.168.1.50', '0.0.0.0', 'gt', 1024, 'any', NULL, 'eq', 53, 0),

-- 3. Cho phép dải 172 truy cập các tài nguyên IP khác (Lệnh 'permit ip any')
(2, 30, 'permit', 'ip', '172.16.0.0', '0.0.255.255', NULL, NULL, 'any', NULL, NULL, NULL, 0);
-- 3. CHUẨN BỊ TÀI NGUYÊN IP (nat_pools)
-- Tạo 2 dải IP Public
INSERT INTO nat_pools (pool_id, nat_id, pool_name, start_ip, end_ip, netmask, success)
VALUES 
(1, 2, 'POOL_VIP', '8.8.8.10', '8.8.8.20', '255.255.255.0', 0),
(2, 2, 'POOL_NORMAL', '9.9.9.100', '9.9.9.200', '255.255.255.0', 0);

-- 4. KHAI BÁO CỔNG MẠNG (nat_interfaces)
-- Giả sử cổng kết nối với LAN là Gi0/2 và cổng ra WAN là Gi0/1
INSERT INTO nat_interfaces (nat_id, interface_name, nat_role, success)
VALUES 
(2, 'GigabitEthernet0/1', 'outside', 0),
(2, 'GigabitEthernet0/2', 'inside', 0);

-- 5. KẾT NỐI THỰC THI (nat_dynamic_rules)
-- Bảng này quyết định "Ai được đi với Pool nào"
INSERT INTO nat_dynamic_rules (id, nat_id, nat_acl_id, pool_id, overload, description, success)
VALUES 
(1, 2, 1, 1, 0, 'Giam doc vao Pool VIP', 0),    -- ACL 1 (GD) gắn với Pool 1 (VIP)
(2, 2, 2, 2, 1, 'Nhan vien vao Pool thuong', 0); -- ACL 2 (NV) gắn với Pool 2 (Normal), có PAT (overload=1)