\chapter{Hướng dẫn cài đặt phần mềm}

Phần này trình bày các bước cài đặt môi trường Python, thư viện phụ thuộc và cách chạy phần mềm.

\begin{lstlisting}[language=bash, caption={Ví dụ cài đặt thư viện}]
uv sync
uv run main.py
\end{lstlisting}
