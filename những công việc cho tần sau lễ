thêm project bar cho app

file thực thi
nhiệm vụ 1 kiểm tra và tự cài uv
nhiệm vụ 2 kiểm tra có python không, nếu chưa có tự python bằng uv
tạo .venv ở thư mục chỉ định
tải thư viện vào .venv bằng uv, sau khi tải xong kiểm tra có tải thiếu thư viện không và cài cái cần thiếu

qt seting
    lưu địa chỉ python trên máy
    sau khi tạo .venv và tải thư viện rồi  đổi đại chỉ python sang .vnev
    lưu địa chỉ backend python
    lưu địa chỉ db
    tạo các biến kiểm tra nhanh {uv, py, .venv, tai thu vien, tạo db}

chỉnh sửa lại .sql
    file data quá lớn rồi cần tạo cấu trúc main.sql và các .sql vệ tinh
    chỉ sửa bảng nat
    copp sql từ rebo cũ sang mới
    tạo cụng sql co interface

python
    file connec thêm chức lưu file cầu hình show run về
    từ đó điến thông tin vào databace

chỉnh lại giao giện và hoàn thiện routing

hoàn thiện qml và ghep code DHCP, ACL

giao việc liên quan tới NAT sau khi file .sql liên quan nat được làm xong

chuẩn bị cho interface

